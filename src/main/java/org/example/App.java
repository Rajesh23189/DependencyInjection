package org.example;

public class App {
     public static void main(String args[])
     {


        /*Dependency Injection(Setter) 8,9 Line Through setter (Manually Injection) , Using SpringCore We Automatically Handel It
                We Inject Dependency Through SetterMethod so, it called SetterDependencyInjection

         Student student = new Student();
         Course course = new Dsa();
         student.setCourse(course);
         */

          /*Dependency Injection(Constructor) 8,9 Line Through Constructor (Manually Injection) Using SpringCore We Automatically Handel It
                We Inject Dependency Through Constructor so, it called Constructor)DependencyInjection

         Student student = new Student( new JavaFullStack());
         student.Study();

           */
     }
}
